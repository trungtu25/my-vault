{
  "name": "personal-vault",
  "version": "1.0.0",
  "description": "Personal Vault - Secure Password Manager",
  "scripts": {
    "build": "cp ../Vault.html www/index.html",
    "sync": "npx cap sync",
    "ios": "npx cap open ios",
    "android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0",
    "@capacitor/android": "^7.0.0",
    "@aparajita/capacitor-biometric-auth": "^10.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
